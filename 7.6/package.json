{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "first tests with Cypress",
  "main": "index.js",
  "scripts": {
    "headedChrome": "npx cypress run --browser chrome --headed",
    "headlessChrome": "npx cypress run --browser chrome",
    "headedElectron": "npx cypress run --browser electron --headed",
    "headlessElectron": "npx cypress run --browser electron"
  },
  "author": "OM",
  "license": "ISC",
  "dependencies": {
    "cypress": "^8.3.0"
  }
}
